const {
  Article,
  NewsArticle,
  ReportageNewsArticle
} = require('./presets/schemas/article')
const { Twitter } = require('./presets/twitter')
const { Facebook } = require('./presets/facebook')

module.exports = {
  Article,
  NewsArticle,
  ReportageNewsArticle,
  Twitter,
  Facebook
}